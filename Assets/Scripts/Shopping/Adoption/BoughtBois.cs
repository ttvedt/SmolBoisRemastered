using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using boiClass;

public class BoughtBois : MonoBehaviour
{
    public static smolBoi[] boughtBoi = new smolBoi[3];
    public static int numPurchased = 0;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
